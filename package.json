{
  "name": "aimexcbot",
  "private": true,
  "workspaces": ["web", "functions"],
  "scripts": {
    "web:dev": "npm run dev --workspace web",
    "web:build": "npm run build --workspace web",
    "web:lint": "npm run lint --workspace web",
    "web:prisma": "npm run prisma:generate --workspace web",
    "func:build": "npm run build --workspace functions",
    "func:prisma": "npm run prisma:generate --workspace functions"
  },
  "dependencies": {
    "@azure/identity": "^4.13.0",
    "@azure/keyvault-keys": "^4.10.0",
    "@azure/keyvault-secrets": "^4.10.0",
    "@prisma/client": "5.19.1",
    "axios": "^1.13.2",
    "crypto-js": "^4.2.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/crypto-js": "^4.2.2"
  }
}
