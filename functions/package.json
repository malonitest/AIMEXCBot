{
  "name": "aimexcbot-functions",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsup runBot/index.ts --format cjs --sourcemap --out-dir dist --target node18 --tsconfig tsconfig.json",
    "start": "npm run build && func start --javascript --prefix dist",
    "prisma:generate": "prisma generate --schema ../prisma/schema.prisma",
    "test": "echo 'Add function tests'"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@azure/identity": "^4.4.1",
    "@azure/keyvault-secrets": "^4.7.0",
    "@azure/keyvault-keys": "^4.8.0",
    "@prisma/client": "^5.19.1",
    "axios": "^1.7.9",
    "pg": "^8.12.0",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "prisma": "^5.19.1",
    "tsup": "^8.3.0",
    "typescript": "^5.6.3"
  }
}
